{
  "name": "@chess/electron",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build && electron .",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@chess/backend": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.9.3",
    "electron": "^30.0.0"
  }
}
